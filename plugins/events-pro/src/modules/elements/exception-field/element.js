/**
 * External dependencies
 */
import React from 'react';
import PropTypes from 'prop-types';

const ExceptionField = ( props ) => {
	return (
		<fieldset></fieldset>
	);
};

ExceptionField.propTypes = {};

export default ExceptionField;
